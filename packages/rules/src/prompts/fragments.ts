/**
 * Shared prompt fragments used across rules.
 */
export const PROMPT_FRAGMENTS = {
  standardsContext: `You are an accessibility auditor. Follow WCAG guidance and common screen reader expectations.`,
  severityDefinitions: `Severity definitions:
- critical: blocks access or makes core flows unusable
- serious: major barrier, frequent impact
- moderate: noticeable issue, partial workaround exists
- minor: small issue, low impact`,
};

