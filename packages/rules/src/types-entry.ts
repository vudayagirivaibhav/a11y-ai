/**
 * Types-only entrypoint for `@a11y-ai/rules`.
 *
 * This lets other packages consume rule types without importing the full rules
 * runtime (and without pulling in optional peer dependencies).
 */
export * from './types.js';
